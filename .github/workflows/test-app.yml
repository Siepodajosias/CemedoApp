name: Cypress Test
on: [push]

jobs:
  cypress-run:
    run-on: ubuntu-latest
    steps:
      -name: Checkout
      -uses: actions/checkout@v2
      -name: Cypress run
      -run: npm run build
      -uses: cypress-io/github-action@v2



